"use strict";
export function HealthCardModel(data, title, createdOn, verifiedOn, id) {
  this.data = data;
  this.title = title;
  this.createdOn = createdOn;
  this.verifiedOn = verifiedOn;
  this.id = id;
}
