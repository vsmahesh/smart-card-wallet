import { ScanHealthCardView } from "../../src/js/views/scan-health-card";

describe("ScanHealthCardView", () => {
  it("must be defined", () => {
    expect(ScanHealthCardView.onScanComplete).toBeDefined();
  });
});
